# Python dependencies for Cybersec Dask air-gap image
# These are pre-installed in the container to avoid runtime pip install
#
# Based on: infra/aws/ansible/roles/panel-viz/files/requirements-panel.txt
# Pinned versions for reproducibility in air-gap environments
#
# IMPORTANT: Dask/distributed/cloudpickle versions MUST match exactly
# between workers, scheduler, and panel-viz. Version mismatch causes
# Scheduler.identity() incompatibility errors.

# Core Dask stack - pinned for serialization compatibility
dask[complete]==2025.2.0
distributed==2025.2.0
cloudpickle==3.1.2

# Data processing - pinned to match cluster versions
pandas==2.2.3
numpy==2.1.3
pyarrow==18.1.0

# Visualization (HoloViews stack)
holoviews>=1.18.0,<2.0.0
datashader>=0.16.0,<1.0.0
hvplot>=0.9.0,<1.0.0
bokeh>=3.3.0,<4.0.0
panel>=1.3.0,<2.0.0
colorcet>=3.0.0,<4.0.0
param>=2.0.0,<3.0.0

# Graph analysis for topology visualization
networkx>=3.0,<4.0

# Storage
fastparquet>=2024.2.0
fsspec>=2024.2.0
s3fs>=2024.2.0
boto3>=1.34.0

# OTel protocol definitions (for OTLP parsing)
opentelemetry-proto>=1.20.0,<2.0.0
protobuf>=4.0.0,<5.0.0

# Utilities
tqdm>=4.66.0,<5.0.0

# Numba for JIT compilation (datashader dependency)
numba>=0.60.0,<1.0.0

# Image processing (fallback rendering)
Pillow>=10.0.0

# Jupyter integration
jupyterlab>=4.0.0,<5.0.0
jupyterhub>=5.0.0,<6.0.0
ipywidgets>=8.0.0,<9.0.0
