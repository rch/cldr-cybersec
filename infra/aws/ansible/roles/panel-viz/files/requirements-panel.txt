# Panel visualization dependencies
# MUST match Dask worker requirements for serialization compatibility

# Dask distributed - MUST match cluster version exactly
# Version mismatch causes Scheduler.identity() incompatibility
dask[complete]==2025.2.0
distributed==2025.2.0
cloudpickle==3.1.2

# Data processing - MUST match Dask cluster versions exactly
numpy==2.1.3
pandas==2.2.3
pyarrow==18.1.0

# Core visualization stack for HoloViews + Datashader
holoviews>=1.18.0
datashader>=0.16.0
hvplot>=0.9.0
bokeh>=3.3.0
panel>=1.3.0
colorcet>=3.0.0
param>=2.0.0

# S3 access
s3fs>=2024.2.0
boto3>=1.34.0

# Numba for datashader acceleration
numba>=0.60.0

# Image processing (fallback rendering)
Pillow>=10.0.0
