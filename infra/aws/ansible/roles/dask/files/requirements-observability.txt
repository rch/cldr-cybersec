# Standalone requirements for OTel observability stack
# Deploy to Dask cluster in K8s - NO Flink dependencies
#
# Install: pip install -r requirements-observability.txt

# Core Dask stack
dask[complete]>=2025.2.0
distributed>=2025.2.0

# Data processing
pandas>=2.0.0
numpy>=2.0.0
pyarrow>=15.0.0

# Visualization (HoloViews stack)
holoviews>=1.18.0
datashader>=0.16.0
bokeh>=3.3.0
panel>=1.3.0
colorcet>=3.0.0
param>=2.0.0

# Graph analysis for topology visualization
networkx>=3.0

# Storage
fastparquet>=2024.2.0
fsspec>=2024.2.0
s3fs>=2024.2.0

# OTel protocol definitions (for OTLP parsing)
opentelemetry-proto>=1.20.0
protobuf>=4.0.0

# Utilities
tqdm>=4.66.0
