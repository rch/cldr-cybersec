# Core Dask - pinned for serialization compatibility
dask[complete]==2025.2.0
distributed==2025.2.0
cloudpickle==3.1.2

# Data Processing - MUST be pinned to exact versions for Dask serialization
# Mismatched versions between client and workers cause deserialization errors
pandas==2.2.3
numpy==2.1.3
pyarrow==18.1.0

# Visualization (HoloViews stack for distributed Datashader)
holoviews>=1.18.0
datashader>=0.16.0
hvplot>=0.9.0
bokeh>=3.3.0
panel>=1.3.0
colorcet>=3.0.0

# Parallel Processing
mpire>=2.8.0

# Storage formats
fastparquet>=2024.2.0
h5py>=3.10.0
zarr>=2.16.0
xarray>=2024.1.0

# Utilities
tqdm>=4.66.0
fsspec>=2024.2.0

# S3 Storage (for AWS)
s3fs>=2024.2.0
aiobotocore>=2.7.0

# OTel Observability
networkx>=3.0
opentelemetry-proto>=1.20.0
param>=2.0.0
